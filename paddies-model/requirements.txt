requests
numpy
shapely
matplotlib
pillow
scipy
xarray
# NetCDF backend so xarray can read the bundled Landsat kelp source
# (data/landsat_kelp_ca.nc). Never imported directly — don't prune it.
netCDF4
pydap
# COG reader for the Sentinel-2 current-canopy fetch (fetch_kelp_sentinel2.py):
# windowed HTTP-range reads of the public AWS sentinel-cogs bucket, no auth.
rasterio
