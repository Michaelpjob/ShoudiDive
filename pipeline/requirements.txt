numpy>=1.26
xarray>=2024.1
netCDF4>=1.6
Pillow>=10.0
requests>=2.31

# wind layer (HRRR GRIB2)
cfgrib>=0.9.10
eccodes>=2.34
scipy>=1.11

# coastline / island clipping
shapely>=2.0

# validation pipeline — prose extraction + HTML parsing
beautifulsoup4>=4.12
anthropic>=0.40

# Copernicus Marine (CMEMS) — independent EU chl source in chl_blend.py.
# Toolbox reads COPERNICUSMARINE_SERVICE_USERNAME/PASSWORD from env; the
# source is skipped when unset, so this dep is inert until the secrets exist.
copernicusmarine>=2.0,<3
